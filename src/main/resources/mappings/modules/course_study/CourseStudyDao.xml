<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.course_study.dao.CourseStudyDao">
    
	<sql id="courseStudyColumns">
		a.id AS "id",
		a.course_id AS "courseId",
		a.train_by_id AS "trainById",
		a.study_status AS "studyStatus",
		a.study_time AS "studyTime",
		a.study_date AS "studyDate",
		a.study_start_time AS "studyStartTime",
		a.study_end_time AS "studyEndTime",
		a.name AS "name",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="courseStudyJoins">
	</sql>
    
	<select id="get" resultType="CourseStudy">
		SELECT 
			<include refid="courseStudyColumns"/>
		FROM course_study a
		<include refid="courseStudyJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="CourseStudy">
		SELECT 
			<include refid="courseStudyColumns"/>
		FROM course_study a
		<include refid="courseStudyJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="CourseStudy">
		SELECT 
			<include refid="courseStudyColumns"/>
		FROM course_study a
		<include refid="courseStudyJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO course_study(
			id,
			course_id,
			train_by_id,
			study_status,
			study_time,
			study_date,
			study_start_time,
			study_end_time,
			name,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{courseId},
			#{trainById},
			#{studyStatus},
			#{studyTime},
			#{studyDate},
			#{studyStartTime},
			#{studyEndTime},
			#{name},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE course_study SET 	
			course_id = #{courseId},
			train_by_id = #{trainById},
			study_status = #{studyStatus},
			study_time = #{studyTime},
			study_date = #{studyDate},
			study_start_time = #{studyStartTime},
			study_end_time = #{studyEndTime}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE course_study SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>