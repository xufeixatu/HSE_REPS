<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.adm.dao.ShopAdminDao">
    
	<sql id="shopAdminColumns">
		a.shop_admin_id AS "shopAdminId",
		a.shop_admin_name AS "shopAdminName",
		a.shop_admin_password AS "shopAdminPassword"
	</sql>
	
	<sql id="shopAdminJoins">
	</sql>
    
	<select id="get" resultType="ShopAdmin">
		SELECT 
			<include refid="shopAdminColumns"/>
		FROM shop_admin a
		<include refid="shopAdminJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="ShopAdmin">
		SELECT 
			<include refid="shopAdminColumns"/>
		FROM shop_admin a
		<include refid="shopAdminJoins"/>
		<where>
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="ShopAdmin">
		SELECT 
			<include refid="shopAdminColumns"/>
		FROM shop_admin a
		<include refid="shopAdminJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO shop_admin(
			shop_admin_id,
			shop_admin_name,
			shop_admin_password
		) VALUES (
			#{shopAdminId},
			#{shopAdminName},
			#{shopAdminPassword}
		)
	</insert>
	
	<update id="update">
		UPDATE shop_admin SET 	
			shop_admin_id = #{shopAdminId},
			shop_admin_name = #{shopAdminName},
			shop_admin_password = #{shopAdminPassword}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM shop_admin
		WHERE id = #{id}
	</update>
	
</mapper>