<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.actcard.dao.ActcardUnsafeDao">
    
	<sql id="actcardUnsafeColumns">
		a.id AS "id",
		a.unsafe_event_id AS "unsafeEventId",
		a.actcard_id AS "actcardId",
		a.parent_id AS "parent.id",
		a.parent_ids AS "parentIds",
		a.name AS "name",
		a.sort AS "sort",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="actcardUnsafeJoins">
	</sql>
    
	<select id="get" resultType="ActcardUnsafe">
		SELECT 
			<include refid="actcardUnsafeColumns"/>
		FROM actcard_unsafe a
		<include refid="actcardUnsafeJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="ActcardUnsafe">
		SELECT 
			<include refid="actcardUnsafeColumns"/>
		FROM actcard_unsafe a
		<include refid="actcardUnsafeJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="ActcardUnsafe">
		SELECT 
			<include refid="actcardUnsafeColumns"/>
		FROM actcard_unsafe a
		<include refid="actcardUnsafeJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findListByActcardId" resultType="ActcardUnsafe">
		SELECT 
			<include refid="actcardUnsafeColumns"/>
		FROM actcard_unsafe a
		<include refid="actcardUnsafeJoins"/>
		<where>
			a.del_flag = '0'
			and a.actcard_id = #{actcardId}
		</where>		
	</select>
	<select id="findUnsafeEvent" resultType="ActcardUnsafe">
		SELECT 
			<include refid="actcardUnsafeColumns"/>
		FROM actcard_unsafe a 
		<include refid="actcardUnsafeJoins"/>
		<where>
			a.del_flag = '0'
			and a.actcard_id in (
			<foreach item="item" index="index" collection="listIds" open="(" separator="," close=")">  
			  #{item}  
			 </foreach>)  
			and a.unsafe_event_id = #{eventid}
		</where>		
	</select>
	
	<insert id="insert">
		INSERT INTO actcard_unsafe(
			id,
			unsafe_event_id,
			actcard_id,
			parent_id,
			parent_ids,
			name,
			sort,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{unsafeEventId},
			#{actcardId},
			#{parent.id},
			#{parentIds},
			#{name},
			#{sort},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE actcard_unsafe SET 	
			unsafe_event_id = #{unsafeEventId},
			actcard_id = #{actcardId},
			parent_id = #{parent.id},
			parent_ids = #{parentIds},
			name = #{name},
			sort = #{sort},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE actcard_unsafe SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>