<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.actcard.dao.ActcardReviewDao">
    
	<sql id="actcardReviewColumns">
		a.id AS "id",
		a.content AS "content",
		a.actcard_id AS "actcard.id",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="actcardReviewJoins">
	</sql>
    
	<select id="get" resultType="ActcardReview">
		SELECT 
			<include refid="actcardReviewColumns"/>
		FROM actcard_review a
		<include refid="actcardReviewJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="ActcardReview">
		SELECT 
			<include refid="actcardReviewColumns"/>
		FROM actcard_review a
		<include refid="actcardReviewJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="actcard != null and actcard.id != null and actcard.id != ''">
				AND a.actcard_id = #{actcard.id}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="ActcardReview">
		SELECT 
			<include refid="actcardReviewColumns"/>
		FROM actcard_review a
		<include refid="actcardReviewJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO actcard_review(
			id,
			content,
			actcard_id,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{content},
			#{actcard.id},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE actcard_review SET 	
			content = #{content},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE actcard_review SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>